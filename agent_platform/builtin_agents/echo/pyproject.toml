[project]
name = "platform-echo"
version = "0.1.0"
description = "Credential-free echo agent used to exercise the platform end to end."
license = { text = "Apache License 2.0" }
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "google-adk>=1.0.0",
    "google-genai>=1.9.0",
]
